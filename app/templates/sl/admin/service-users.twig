{% extends 'sl/admin/master.twig' %}

{% block section %}
  <h1 class="title is-1">Asignar nuevo administrador
  </h1>
<h2 class="subtitle is-4">
    Aqui podrá asignar un nuevo administrador. Note que el usuario debe estar registrado previamente en la plataforma.
</h2>
  <hr>
<admin-assign-service-user get-candidates-url="{{path_for('getCandidatos')}}" run-service-user="{{path_for('runNewServiceUser')}}" run-remove-service-user="{{path_for('runRemoveServiceUser')}}" :currents="{{service_users|json_encode}}">
{% include 'loading.twig' %}
</admin-assign-service-user>
{% endblock %}